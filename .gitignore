# Server-Datenbestand (wird zur Laufzeit erzeugt)
data/

# Android-Build
android/.gradle/
android/build/
android/app/build/
android/local.properties
# Gebündelte Web-App (wird beim Build aus dem Repo-Root kopiert)
android/app/src/main/assets/web/

node_modules/
